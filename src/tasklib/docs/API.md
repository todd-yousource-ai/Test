# tasklib API Reference

This document specifies every public interface in tasklib. All type annotations use Python type-hint notation. All examples are illustrative -- they show intended behavior once the corresponding implementation layers have been merged.

---

## TaskStatus Enum

**Module:** `tasklib.models.task`

`TaskStatus` is a string enum that represents the lifecycle state of a task.

| Value          | Description                                      |
|